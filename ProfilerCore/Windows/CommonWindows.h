#pragma once

#include "PublicCommonWindows.h"

// MEMORY //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#define BRO_ALIGN(N) __declspec( align( N ) )
#define MALLOC_ALIGN(ALIGNMENT, SIZE) _aligned_malloc(ALIGNMENT, SIZE)
#define FREE_ALIGN(PTR) _aligned_free(PTR)
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

// OS SPECIFIC /////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#define THREAD_LOCAL_VARIABLE __declspec(thread)
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////